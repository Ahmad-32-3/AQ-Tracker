# AQ-Tracker-Copy — Phase Plan

Source: full chat vision (detective alley → coffee map → islands → metrics).

**Snapshots**
- `C:\dev\AQ-Tracker-Copy-snapshots\lamp-swing-v1` — lamp only, no detective
- `C:\dev\AQ-Tracker-Copy-snapshots\detective-lamp-v1` — dim alley + detective holding lamp (current before Phase 1)

---

## Done (foundation)

| ID | What |
|----|------|
| F1 | Fork `AQ-Tracker-Copy` (main reserved) |
| F2 | Ragged cartoon brick alley |
| F3 | Wall scrolls left (fake walk) |
| F4 | Dim ambient (lamp does the reveal) |
| F5 | Swinging lamp + intensity (physics-ish) |
| F6 | Detective silhouette **holding** the lamp |

---

## Remaining phases (execute one at a time)

### Phase 1 — Coffee map screen ✅
Parchment + décor + 3 ink landmarks (Lahore / Karachi / Islamabad).

### Phase 2 — Alley beat → smoke → zoom → map ✅
Walk → gutter smoke → wall map → engulf → zoom → coffee map reveal.

### Phase 3 — Island → clouds → metrics ✅
Click landmark → **black fog** → city metrics (Open-Meteo + A1–A12). Back returns to map through fog.

### Phase 4 — Map is home ✅
After intro, **map is the landing page**. Back from city → **map** (not alley). Dev phase bar removed.

### Phase 5 — Replay intro ✅
Top-left **Replay intro** replays alley → smoke → zoom → map.

### Phase 6 — Polish (optional)
Look-up / map-on-wall beat refinements, magnifier, art upgrades, reduced-motion.

---

## How we work
Finish one phase → you review → then the next. No skipping ahead unless you ask.
